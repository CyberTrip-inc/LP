---
import Base from '../../../layouts/Base.astro';
import Header from '../../../components/Header.astro';
import AnnounceBanner from '../../../components/AnnounceBanner.astro';
import Footer from '../../../components/Footer.astro';
import { getCollection } from 'astro:content';

const lang = 'en';

const allNews = await getCollection('news');
const noticeItems = allNews
  .filter(n => n.data.category === 'notice')
  .sort((a, b) => b.data.date.localeCompare(a.data.date));
---

<Base
  title="Public Notice — CyberTrip"
  description="Official public notices from CyberTrip, Inc."
  lang={lang}
  page="notice"
>
  <AnnounceBanner lang={lang} />
  <Header lang={lang} page="notice" />

  <section class="page-hero page-hero--news">
    <div class="page-hero-inner">
      <p class="page-hero-label">Public Notice</p>
      <h1 class="page-hero-title">Public Notice</h1>
      <p class="page-hero-sub">
        Official public notices from CyberTrip, Inc.
      </p>
    </div>
  </section>

  <section class="section section--dark">
    <div class="section-inner">
      <div class="section-label-line">
        <span class="section-label">Notices</span>
      </div>
      {noticeItems.length > 0 ? (
        <div class="news-list">
          {noticeItems.map((item) => {
            const title = item.data.title_en || item.data.title;
            const badge = item.data.badge_en || item.data.badge;
            const linkHref = item.data.link ? `/en${item.data.link}` : undefined;
            const linkText = item.data.link_text_en || item.data.link_text;
            return (
              <div class="news-item">
                <div class="news-item-date">{item.data.date}</div>
                <div class="news-item-content">
                  {badge && <span class="news-item-badge news-item-badge--notice">{badge}</span>}
                  <span class="news-item-title">{title}</span>
                  {linkHref && linkText && (
                    <a href={linkHref} class="news-item-link">{linkText} &rarr;</a>
                  )}
                </div>
              </div>
            );
          })}
        </div>
      ) : (
        <p class="news-empty">No public notices at this time.</p>
      )}
    </div>
  </section>

  <Footer lang={lang} />
</Base>
